public static boolean isCyclic(ListNode head){
	ListNode fast = head;
	ListNode slow = head;
	
	while(fast != null && slow != null){
		fast = first.next;
		second = slow.next.next;
		
		if(fast == slow){
			return true;
		}
	}
	
	return false;
}
